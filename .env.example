# Telegram Bot Configuration
TG_BOT_TOKEN="YOUR_TELEGRAM_BOT_TOKEN"
ADMIN_IDS="YOUR_ADMIN_TELEGRAM_ID1,YOUR_ADMIN_TELEGRAM_ID2" # Comma-separated

# TeamTalk Server Configuration
HOST_NAME="your_teamtalk_server_host"
PORT="10333"
UDP_PORT="10333" # Optional, defaults to TCP_PORT
USER_NAME="your_bot_teamtalk_username"
PASSWORD="your_bot_teamtalk_password"
NICK_NAME="RegisterBot"
CLIENT_NAME="PyTalkRegisterBot"
ENCRYPTED="0" # 0 for false, 1 for true
SERVER_NAME="My TeamTalk Server" # Display name for the server

# Registration Settings
VERIFY_REGISTRATION="0" # 0 for false (auto-approve), 1 for true (admin approval)
LANG="0" # Default bot language: 0 for English, 1 for Russian

# Flask Web Registration Configuration
FLASK_REGISTRATION_ENABLED="1" # 0 for false, 1 for true
FLASK_HOST="0.0.0.0"
FLASK_PORT="5000"
FLASK_SECRET_KEY="your_very_secret_flask_key_here" # Important for session management

# Flask SSL (Optional)
FLASK_SSL_ENABLED="0" # 0 for false, 1 for true
FLASK_SSL_CERT_PATH="" # Path to SSL certificate (e.g., /path/to/cert.pem)
FLASK_SSL_KEY_PATH=""  # Path to SSL private key (e.g., /path/to/key.pem)

# TeamTalk Client Template for Flask Downloads (Optional)
# Path to a directory structured like a TeamTalk client installation
# e.g., templates/TeamTalk_v5.9.0_Windows_x64/
# Inside this dir, there should be a Client/TeamTalk5.ini file.
TEAMTALK_CLIENT_TEMPLATE_DIR=""